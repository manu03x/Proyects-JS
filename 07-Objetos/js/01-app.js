const nombre = 'Monitor 20 pulgadas';
const precio = 300;
const disponible = true;

//Un objeto agrupa todo en una sola variable

// Object literal

const producto = {
    nombre:'Monitor 20 pulgadas',
    precio: 300,
    disponible: true,
}

console.log(producto);

// En lugar de tener 3 variables podemos crear un objeto que contenga esos mismos tres valores
